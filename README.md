# 轻剪 · 对话式智能剪辑 H5

添加本地视频、图片或音频，用对话生成剪辑方案、封面、口播和成片。上传视频后，FFmpeg 自动找出切镜点并显示镜头缩略图；可用对话挑选镜头、拼接片段并加入上传的音乐或内置轻快配乐。口播可以单独试听与下载，也可以通过对话加入 MP4。所有生成任务以后台任务执行，刷新页面后仍可查看结果或重试。

![轻剪 H5 视觉方向](docs/h5-concept.png)

## 启动

需要 Node.js 24 和 pnpm。首次运行：

```bash
pnpm install
pnpm dev
```

打开 <http://127.0.0.1:5173/>，管理后台在 <http://127.0.0.1:5173/admin>。开发时 H5 使用 Vite 的 5173 端口，API 使用 8787 端口。正式本机运行可先执行 `pnpm build`，再执行 `pnpm start`，通过 <http://127.0.0.1:8787/> 访问。

首页为 Landing，注册时填写显示名称、邮箱与至少 10 位密码；登录后进入当前帐号的对话工作区。用户中心可修改密码或退出登录。帐号密码哈希和登录会话保存在 `data/auth.json`，会话、素材与成片按帐号隔离；该文件和模型配置一样不提交到 Git。当前未接入邮箱验证或自助密码找回。

这台开发机的本地和线上测试帐号记录在 `data/TEST_ACCOUNTS.local.md`。该文件含密码，位于 Git 忽略的 `data/` 目录，仅供本机验收使用；在其他机器上需单独准备测试帐号。

首次启动时，服务端在 `data/admin-token` 生成管理员令牌。仅在本机读取该文件，将令牌填入管理后台。管理后台可维护文本、图片和口播模型的厂商、模型 ID、服务地址、启用状态及 API Key，并设置新对话默认文本模型。API Key 使用本机令牌派生的密钥加密保存在 `data/provider-models.json`，普通页面和管理后台读取接口都不会回传完整密钥。`data/` 不提交到 Git。请妥善备份 `admin-token` 与模型配置文件，两者需要配套恢复。

默认预置的模型条目需要在后台填入你自己的 API Key 才能使用：

| 用途 | 预置模型 |
| --- | --- |
| 对话与剪辑规划 | `doubao-seed-2-1-pro-260915` |
| 图片生成 | `doubao-seedream-5-0-flash-260915` |
| 口播音频 | `seed-audio-1.0` |

## 使用流程

1. 在对话页添加视频、图片或 MP3/WAV/OGG 音频。素材库中可预览视频自动识别的分镜及缩略图，点击镜头会填写剪辑指令。
2. 发送剪辑指令，Pi Agent 读取切镜时间点，通过受校验的工具生成片段方案。继续发送消息调整时长、比例、顺序或片段。切镜检测不等于画面语义理解。
3. 通过对话要求生成口播或图片。口播卡片显示文案和独立音频；图片卡片可预览、下载，结果同时进入素材库。选择一张图片后发送“把这张图片设为封面”，或明确要求把图片加入视频片段。
4. 发送“搜索轻快 BGM”会由 Pi Agent 整理关键词，并显示 Pixabay、24bit 的原站入口。集成方可调用 `/api/music/search` 等服务端接口，或在用户已打开的原站页面运行 `scripts/music-browser-client.js`，让 Chrome 在同源请求中使用现有会话状态。服务端直连当前仍可能返回 `UPSTREAM_CHALLENGE`；浏览器代码与服务端接口的验证结果见 [BGM 来源调查](docs/BGM_SOURCE_RESEARCH.md)。成功下载并确认许可后，可上传音频作为 BGM。发送“配上轻快背景音乐”仍可使用内置原创合成配乐。发送“把口播加入成片”后，再发送“生成成片”。背景音乐在导出时循环并降低音量，口播与原声可同时混音。封面会用于 H5 的成片预览卡片，原图与成片版本一同保存在服务端。
5. 预览、进入剪辑拼接页查看时间线，从成片库下载对应版本的 MP4。导出失败可重试。

所有剪辑和生成请求都由对话发起。页面里的快捷操作只填写指令，发送后才执行。服务端用 `@earendil-works/pi-agent-core` 的 Agent Loop 规划内容，FFmpeg 负责确定性剪辑与音轨合成。当前 Agent 只能读取素材名称、格式和时长，没有画面理解能力。

## 当前范围

- 帐号数据保存在服务端，同一帐号可跨设备继续创作；API 仅监听 `127.0.0.1` 并由 Nginx 提供 HTTPS 入口。当前未提供邮箱验证、自助密码找回或存储配额。
- 一次最多上传 6 个视频、图片或音频，单文件最大 300 MB；图片支持 PNG、JPEG、WebP，音频支持 MP3、WAV、OGG。剪辑方案最多 8 个片段、60 秒，支持 9:16、16:9、1:1。视频分镜最多展示 8 段。
- 对话中的生成任务按消息意图执行。复合请求如同时生成口播和图片，建议分两条消息发送。
- 封面作为 H5 预览海报保存；当前 MP4 文件本身不嵌入封面图作为首帧。
- 文本模型可在后台添加不同厂商的兼容模型；图片和音频生成目前使用内置的火山服务协议，后台可调整模型 ID 与官方服务地址。
- 配置 OSS 后，素材、生成图片、口播音频、成片、封面和分镜缩略图持久保存在私有 Bucket；`data/` 保留 FFmpeg 处理缓存与帐号/任务状态。旧数据可用 `pnpm tsx scripts/sync-oss.ts` 迁移。未配置 OSS 时使用本地存储。删除素材可能使引用它的方案无法再次导出，已生成的成片版本仍可下载。

参见 [PRD](docs/PRD.md)、[开发任务清单](docs/DEVELOPMENT_TASKS.md)、[接口契约](docs/API_CONTRACT.md)、[体验验收记录](docs/ACCEPTANCE.md)、[部署说明](docs/DEPLOYMENT.md)、[功能展示视觉稿](docs/UI_STATES.md) 和 [登录视觉稿](docs/auth-concept.png)。

远程测试环境通过服务器上的 `qingjian-cd.timer` 自动拉取并部署 `main`；部署脚本、构建隔离和回滚方式见 [部署说明](docs/DEPLOYMENT.md)。

音乐站点浏览器抓包、接口示例与重放边界见 [BGM 来源调查](docs/BGM_SOURCE_RESEARCH.md)。可运行 `node scripts/probe-bgm-sources.mjs` 检查当前网络下的站点状态；探测不复用个人浏览器 Cookie，也不绕过站点防护。
