# 轻剪开发与体验验收记录

日期：2026-09-25 起。范围：H5、Pi Agent 服务、火山图片与口播模型、FFmpeg 导出及模型管理后台。验收使用真实素材和模型返回文件；测试密钥仅写入被 Git 忽略的本机 `data/` 与远端受限的数据目录，不记录在本文档中。

## 开发任务结果

| 任务 | 结果 | 证据 |
| --- | --- | --- |
| T01 数据与任务模型 | 完成 | 会话、消息、素材、任务、产物与设置持久化；服务重启后恢复未完成任务 |
| T02 素材接入 | 完成 | 上传真实 MP4/PNG，素材库选择图片后输入框显示附件；伪装为 PNG 的文本文件返回 400 |
| T03 对话式 Pi Agent | 完成 | 真实文本模型通过 Agent Loop 提交受校验片段方案；导出由对话触发 |
| T04 口播 | 完成 | 生成可阅读文案与独立 MP3；浏览器试听、下载，加入成片后导出视频含音轨 |
| T05 图片 | 完成 | 文生图、参考图生成真实 JPEG；图片作为任务产物进入素材库，失败任务可重试 |
| T06 成片 | 完成 | FFmpeg 输出可播放 MP4；预览与下载接口均返回实际文件，版本可追溯 |
| T07 移动端 H5 | 完成 | 390×844 与 320×640 视口无横向溢出；对话、菜单、素材库、成片库、设置、时间线可操作 |
| T08 管理后台 | 完成 | 管理令牌校验、模型新增/编辑/禁用/删除、默认文本模型；读接口不返回完整密钥 |
| T09 异常态 | 完成 | 无素材、空消息、非法文件、无效设置、无效资源和失败重试有明确反馈 |
| T10 交付与验收 | 完成 | 构建、真实模型及浏览器旅程、异常态、差距记录见下文 |

## 核心用户旅程

1. 上传一段 4 秒旅行 MP4 和一张参考 PNG，发送“剪成 3 秒竖屏”的指令。Pi Agent 返回 1 个片段、9:16、3 秒方案。
2. 发送口播请求。文本模型返回“把温柔，藏进这场旅行里。”，音频模型返回约 3.9 秒 MP3；页面可播放、暂停、拖动进度并下载。
3. 发送“生成成片，并把刚才的口播加入视频”。FFmpeg 输出约 3.02 秒 MP4；文件同时包含 H.264 视频轨与 AAC 音频轨。浏览器的成片卡、剪辑拼接页和成片库可预览与下载。
4. 发送封面生成请求。文生图返回 JPEG；同一张参考 PNG 的图生图请求在延长模型等待窗口后真实返回 JPEG。失败后的重试沿用原消息与附件；完整应用中的重试产物为 700,186 字节 JPEG，预览与下载接口均返回 HTTP 200。
5. 继续说“把当前片段缩短到 2 秒”，Pi Agent 生成方案 v2；再发送重新生成成片，得到约 2 秒 MP4。v1、v2 文件均保留、可下载，且都有视频与音频轨。随后导出的 v3 保存了当时的方案快照和口播标记；从成片库进入时间线时显示对应的 2 秒片段与音轨。较早生成、没有快照的本机测试版本可播放和下载，并显示明确提示。
6. 选择参考图片并通过对话设为成片封面，后续导出版本保留独立封面文件。封面接口返回 HTTP 200 与原图 MIME 类型；H5 用它作为成片预览海报。只修改封面的指令不会自动导出。
7. 明确要求“把这张图片作为片段”，Pi Agent 将当前消息附加的生成图片放在视频片段之后，各占 2 秒；方案校验阻止误用素材库里的另一张图片。导出的 v5 为约 4.02 秒 MP4，含 H.264、AAC、2 个片段及独立封面文件。未明确要求图片进入视频时，图片仅可作封面或视觉参考。
8. 刷新页面仍能看到历史、任务和产物；独立服务重启后仍能读取并继续处理已保存任务。

## 功能与异常态检查

| 场景 | 结果 |
| --- | --- |
| 管理 API 缺少令牌 | HTTP 401 |
| 非法语言、不可用默认模型 | HTTP 400，原设置未被接受 |
| 空消息、错误会话、缺失素材/任务/产物 | 分别返回 HTTP 400/404 与可读错误 |
| 文件内容与声明图片类型不符 | HTTP 400，拒绝入库 |
| 模型新增、编辑、禁用、删除 | 管理接口可完成；公开和管理读取接口均不含 `apiKey` 字段 |
| 默认模型 | 新对话继承当前默认文本模型，既有会话保留原模型 |
| 语言切换 | 中英界面立即切换；刷新后保持选择 |
| 英文快捷指令 | 导出、口播、封面、裁剪与移除口播分流到预期任务类型 |
| 图片用途 | 明确的封面指令只更新方案；明确的图片片段指令才允许图片进入视频画面 |
| 片段校验 | 单独校验覆盖了“未明确请求图片片段时拒绝”“明确请求时接受”“选错附件图片时拒绝” |
| 对话背景 | 预设暖色图片可选择、恢复默认；仅影响对话区域 |
| 模型失败 | 用户请求、附件和已有产物保留，任务显示错误及重试入口 |
| 从素材库返回 | 图片附件出现在输入框内；返回对话后保留原滚动位置 |

## 体验验收

- **上手成本：**首页给出示例指令；添加素材后可直接输入自然语言。口播、封面和成片均在同一对话完成。首次使用需管理员先配置三个模型密钥，这一步目前通过独立后台完成。
- **移动端体验：**实测 320px 与 390px 宽度没有横向滚动；底部输入框常驻，附件缩略图、任务状态和媒体卡片在单列布局中可读。真实音频可在卡片内试听，视频以适合竖屏素材的方式展示。
- **交互流畅度：**提交消息立即返回任务并保持对话可用；离开页面、刷新后可继续看任务。参考图生成实测约 184 秒，长时间阶段只有任务状态与阶段进度，没有准确剩余时间。
- **完成度差距：**当前为单用户测试部署，使用共享 Basic Auth，没有账号登录、跨设备同步或批量数据删除。Agent 不理解画面内容；切镜基于画面变化，一条消息只触发一个主生成任务，需分步提出复合需求。用户协议与隐私政策页面提供原型说明，正式上线前仍需运营方审定正式文本。没有弱网节流条件下的专项时延数据。
- **封面边界：**封面在 H5 预览中显示并与成片版本一起保存，MP4 本身未将封面合成为首帧。

## 复现命令

```bash
pnpm build
pnpm dev
```

访问 <http://127.0.0.1:5173/> 与 <http://127.0.0.1:5173/admin>。管理员令牌位于本机 `data/admin-token`。不要将密钥、令牌或 `data/` 提交到公开仓库。

## 2026-09-26 分镜、BGM 与远端验收

- `docs/test.mp4` 为 30.05 秒、854×480 的 H.264/AAC 素材。上传后自动检测到 8 个镜头，分界点约为 2.75、5.13、10.83、13.63、19.08、25.25、27.88 秒；8 张代表帧均可经 HTTPS 打开。检测依据画面变化，不产生画面语义描述。
- Pi Agent 按指令选取前 4 个镜头，生成 16:9、约 13.6 秒的可校验方案；FFmpeg 顺序拼接并混入内置轻快配乐。实际 MP4 为 13.638672 秒、1,241,752 字节，包含 H.264 视频轨和 AAC 音轨，HTTPS 下载返回 200。单独上传 WAV 后，选择其作为 BGM 并重新导出也通过。
- 远端位于 `https://101.47.18.93/qingjian/`，Nginx Basic Auth 保护测试页面；管理 API 另由管理员 Bearer 令牌保护。API 与成片只在 `/qingjian/` 下，原有服务器首页继续返回 200。服务进程以 `qingjian` 系统用户运行，绑定 `127.0.0.1:8787`，数据目录权限为 700，令牌和加密模型配置文件权限为 600。
- 远端异常态：无 Basic Auth 的页面 API 返回 401；无 Bearer 令牌的管理 API 返回 401；不存在的镜头返回 404；伪装为 MP3 的无效音频返回 400，未进入素材库。
- Playwright 在 1365×900 和 390×844 视口验证页面加载、成片进入剪辑拼接页、背景音乐轨显示、素材库 8 张镜头图可见；无控制台错误和横向溢出。触发镜头卡片后，输入框预填含正确时间段的剪辑指令。
- 当前内置 BGM 是原创程序合成音，适合流程验收；正式创作建议上传用户授权的配乐。测试部署的共享访问密码不等同于正式多用户鉴权。

## 2026-09-26 音乐来源接入验收

- 首轮本地浏览器抓包：Pixabay 中文搜索使用 `/zh/music/search/<关键词>/` 页面；24bit 搜索触发两个 `POST /api/player/searchOnlineMusicOne|Two`，首轮返回 522，随后另一轮返回 403。无浏览器会话的 Node 搜索探测也被拦；Pixabay 单曲 CDN MP3 可直接下载。后续在原站 Chrome 会话内以 `credentials: include` 执行代码请求后，24bit 搜索、详情、授权和完整音频流读取均成功；服务端 Node 路径仍被挑战。详见 [BGM 来源调查](BGM_SOURCE_RESEARCH.md)。
- 本地对话“搜索轻快的 BGM”创建 `music` 任务，Pi Agent 返回关键词和两个原站入口；粘贴 Pixabay 官方 CDN MP3 地址后，服务端下载、识别为 53.52 秒音频并加入素材库。生成的 3.02 秒、9:16 MP4 包含 H.264 与 AAC 音轨，下载返回 200。恶意非 Pixabay 主机地址被拒绝。
- 功能代码 SHA `f264b479def6c34521757b714ba624eab1b4b21e`。真实对话按 `docs/test.mp4` 前四个分镜拼接 16:9 视频，已导入的 Pixabay MP3 保持为当前 BGM；导出的 MP4 为 13.638672 秒、1,241,500 字节，包含 H.264 视频轨与 AAC 音轨。服务本机与公网 `/qingjian/api/download/<id>` 均返回 200、`video/mp4`。公网 H5 返回 200。
- 390×844 视口检查 BGM 结果卡片，两个来源入口、搜索词和导入说明均可见；点击 Pixabay 入口后打开对应搜索页。开发服务器的 Vite HMR WebSocket 在应用内浏览器中出现连接错误，页面和功能可用；生产构建成功，远端静态页可访问。24bit 曲目授权和站点接口可用性仍是未完成项。

## 2026-09-26 网页音乐请求封装

- 按真实浏览器抓包增加 `/api/music/search`、`/api/music/pixabay/detail`、`/api/music/pixabay/download`、`/api/music/24bit/download`。24bit 请求保留两路搜索 POST、关键词 POST 和下载授权 POST 的实际字段；音频代理限制域名和文件大小。Pixabay 搜索解析服务端渲染的曲目卡片，详情页解析官方 CDN MP3 地址。
- `pnpm build` 通过。启动本地服务后请求两站搜索，均返回 HTTP 502、`UPSTREAM_CHALLENGE`，上游状态 403；无效站外详情 URL 返回 HTTP 400、`INVALID_TRACK_URL`。这确认封装和错误呈现工作，不代表两站自动搜索已跑通。浏览器抓包细节及请求示例见 [BGM 来源调查](BGM_SOURCE_RESEARCH.md)。
- 服务端以普通 Node HTTPS 重放抓到的路由与浏览器常见请求头；Node TLS 客户端并非 Chrome。当前网络下需要站点测试环境或测试出口放行后，才能完成服务端自动搜索和 24bit 下载代理的端到端验收。
- 新增 `scripts/test-music-api-flow.mjs` 并实际执行调用链：Pixabay 搜索与详情返回挑战；用抓包得到的曲目 CDN 地址单独调用下载，返回 200、`audio/mpeg`、1,712,796 字节；24bit 搜索返回挑战，未猜测字段继续下载。脚本最终结果为两站 `FULL_FLOW_INCOMPLETE`，并且不把音频写入磁盘。
